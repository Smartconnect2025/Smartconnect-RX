Deploy live for b0b5bf0: fix(H11,H12,C2,M1,M3): auth on check-status, verify-NPI, DigitalRx webhook, debug pages H11: check-status now requires admin/super_admin auth H12: verify-NPI now requires authenticated user C2: DigitalRx webhook validates DIGITALRX_WEBHOOK_SECRET header when configured Removed verbose GET health check leaking API format details M1: debug-pharmacies page checks admin role client-side, redirects unauthorized M3: check-my-role requires admin auth, returns minimal info (no cookie dump) M2: Seed routes already have admin auth — no changes needed
March 2, 2026 at 11:33 PM
Deploy started for b0b5bf0: fix(H11,H12,C2,M1,M3): auth on check-status, verify-NPI, DigitalRx webhook, debug pages H11: check-status now requires admin/super_admin auth H12: verify-NPI now requires authenticated user C2: DigitalRx webhook validates DIGITALRX_WEBHOOK_SECRET header when configured Removed verbose GET health check leaking API format details M1: debug-pharmacies page checks admin role client-side, redirects unauthorized M3: check-my-role requires admin auth, returns minimal info (no cookie dump) M2: Seed routes already have admin auth — no changes needed
Triggered via Deploy Hook
March 2, 2026 at 11:29 PM
Deploy live for 71a76ed: fix(C1,C3,C4): critical security — auth on 6 routes, webhook signature required, encryption hardened C1 — Added authentication + role checks to 6 unprotected API routes: - GET /api/admin/pharmacies/list — admin/super_admin only - POST /api/admin/providers/tier-assignment — admin/super_admin only - GET /api/admin/providers/[id]/tier — admin/super_admin + own provider - POST/PATCH /api/admin/test-prescription-status — admin/super_admin only - POST /api/prescriptions/status-batch — auth required, non-admin scoped to own data - POST/GET /api/patients/[id]/payment-method — auth required, ownership enforced C3 — Authorize.Net webhook signature bypass fixed: - x-anet-signature header now REQUIRED (401 if missing) - AUTHNET_SIGNATURE_KEY env var now REQUIRED (503 if not set) - Internal secret calls check env var before use, log + skip if missing C4 — Encryption fallback hardened: - Production: throws error if ENCRYPTION_KEY missing - Development: warns and uses fallback for local testing only - Key fetched per operation, not cached at module level
March 2, 2026 at 10:47 PM
Deploy started for 71a76ed: fix(C1,C3,C4): critical security — auth on 6 routes, webhook signature required, encryption hardened C1 — Added authentication + role checks to 6 unprotected API routes: - GET /api/admin/pharmacies/list — admin/super_admin only - POST /api/admin/providers/tier-assignment — admin/super_admin only - GET /api/admin/providers/[id]/tier — admin/super_admin + own provider - POST/PATCH /api/admin/test-prescription-status — admin/super_admin only - POST /api/prescriptions/status-batch — auth required, non-admin scoped to own data - POST/GET /api/patients/[id]/payment-method — auth required, ownership enforced C3 — Authorize.Net webhook signature bypass fixed: - x-anet-signature header now REQUIRED (401 if missing) - AUTHNET_SIGNATURE_KEY env var now REQUIRED (503 if not set) - Internal secret calls check env var before use, log + skip if missing C4 — Encryption fallback hardened: - Production: throws error if ENCRYPTION_KEY missing - Development: warns and uses fallback for local testing only - Key fetched per operation, not cached at module level
Triggered via Deploy Hook
March 2, 2026 at 10:43 PM