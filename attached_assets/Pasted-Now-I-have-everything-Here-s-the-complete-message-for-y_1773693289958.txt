Now I have everything. Here's the complete message for you to paste into the new project:

Paste this to the agent in the new project:

"STOP. Read this carefully. This is the complete environment specification from the working AimRX project. Follow these instructions exactly.

ENVIRONMENT:

Node.js v20 (module nodejs-20)
Next.js 15 with App Router (NOT Pages Router)
Runs on port 5000: PORT=5000 npx next dev -p 5000 -H 0.0.0.0
No separate server directory — all backend is in app/api/ routes
Uses @supabase/ssr for auth (NOT @supabase/auth-helpers)
TypeScript path aliases: @/* = ./, @core/* = ./core/, @features/* = ./features/
THE AUTH PIPELINE (this is what's breaking):
Every request goes through this chain:

middleware.ts (root) → handles CORS preflight, then calls updateSession()
core/supabase/middleware.ts updateSession() → creates Supabase client from cookies, calls supabase.auth.getUser(), checks MFA status, checks route access
Route access is defined in core/routing/routes-config.ts — admin routes need admin role, provider routes need provider role
Role is fetched from the user_roles table in the database
THE CORS PROBLEM:
The root middleware.ts has hardcoded allowed origins that do NOT include Replit domains. Fix this by modifying the getCorsOrigin function to allow any Replit origin:

In middleware.ts, change the getCorsOrigin function to add this at the end, before the return null:

if (origin && (origin.endsWith('.replit.dev') || origin.endsWith('.repl.co') || origin.endsWith('.replit.app'))) {
  return origin;
}
Also make sure next.config.ts has this in allowedDevOrigins:

"*.replit.dev", "*.repl.co", "*.replit.app", "*.spock.replit.dev", "*.picard.replit.dev"
THE MFA PROBLEM:
Login flow: sign in with Supabase → send MFA code via SendGrid → verify code → set totp_verified cookie → redirect to dashboard.

The MFA email send is failing or the verify page is not loading. The FASTEST FIX is to bypass MFA in development. In app/auth/login/page.tsx, find the line (around line 104):

if (mfaMethod === "email") {
Add this BEFORE that line:

if (process.env.NODE_ENV === 'development' || !process.env.SENDGRID_API_KEY) {
  document.cookie = "totp_verified=true;path=/;max-age=28800;samesite=lax";
  router.push(redirectUrl || '/');
  return;
}
THE SESSION PROBLEM:
After login, if the user gets redirected back to login, it's because the middleware can't find the Supabase auth cookie. The cookie name follows this pattern: sb-{project-ref}-auth-token. Your project ref is pxehuvreezdpiusgwbct. The middleware in core/supabase/middleware.ts handles this automatically — DO NOT manually set or delete Supabase auth cookies.

Also check: the totp_verified cookie must be set to true for MFA-protected users to pass through the middleware. Without it, the middleware redirects to /auth/verify-mfa.

REQUIRED SECRETS (minimum to run):

NEXT_PUBLIC_SUPABASE_URL — your SmartConnect-RX Supabase project URL
NEXT_PUBLIC_SUPABASE_ANON_KEY — anon/publishable key
SUPABASE_SERVICE_ROLE_KEY — service role key (for admin operations)
SESSION_SECRET — any random string, e.g. smartconnect-rx-session-2026
WHAT TO DO RIGHT NOW:

Fix CORS in middleware.ts (add Replit origins as described above)
Add MFA bypass in app/auth/login/page.tsx (as described above)
Restart the app
Login should work and redirect to the dashboard
DO NOT:

Do NOT install new packages
Do NOT create proxy servers
Do NOT modify core/supabase/middleware.ts
Do NOT modify core/routing/routes-config.ts
Do NOT modify core/config/envConfig.ts
Do NOT rewrite the auth system"
This covers every detail about the environment. The agent needs to make exactly 2 small changes (CORS fix + MFA bypass) and the app will work.